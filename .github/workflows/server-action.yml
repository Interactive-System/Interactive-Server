name: hana-server workflow
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Notify Slack - Deployment Started
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"배포가 시작되었습니다."}' https://hooks.slack.com/services/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

      - name: Gradle Build
        run: ./gradlew bootJar

      - name: Verify Build Output
        run: ls -la build/libs

      - name: Docker Image Build
        env:
          DOCKER_BUILDKIT: 1
        run: docker build --tag rlawjddn5980/hana-server:latest .

      - name: Docker Hub Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Docker Hub Push
        run: docker push rlawjddn5980/hana-server:latest

      - name: Connect to WAS & Execute Application
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.WAS_HOST }}
          username: ${{ secrets.WAS_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            if [ "$(sudo docker ps -q -f name=hana-server)" ]; then
              sudo docker stop hana-server || true
            fi
            if [ "$(sudo docker ps -a -q -f name=hana-server)" ]; then
              sudo docker rm hana-server || true
            fi
            sudo docker rmi rlawjddn5980/hana-server:latest --force || true
            sudo docker pull rlawjddn5980/hana-server:latest
            if [ ! "$(sudo docker network ls -q -f name=hana_network)" ]; then
              sudo docker network create hana_network
            fi
            sudo docker run -d -p 8080:8080 --name hana-server --network hana_network -v /home/ubuntu/logs:/logs rlawjddn5980/hana-server:latest

      - name: Notify Slack - Deployment Finished
        if: success()
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"인터렉티브 하나 서버 배포가 완료되었습니다."}' https://hooks.slack.com/services/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

      - name: Notify Slack - Deployment Failed
        if: failure()
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"인터렉티브 하나 서버 배포에 실패했습니다."}' https://hooks.slack.com/services/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
